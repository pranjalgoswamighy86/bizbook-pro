{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && mkdir -p db && DATABASE_URL='file:./db/custom.db' npx prisma db push --skip-generate && npx next build && node postbuild.js"
  },
  "deploy": {
    "startCommand": "bash scripts/railway-start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
